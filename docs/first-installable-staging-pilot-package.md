# First Installable Staging Pilot Package

## Ziel

Ein erstes real installierbares, aber streng begrenztes Pilotpaket wird nur fuer staging/test vorbereitet.

## Paketprofil

- staging-only
- `pilot` channel
- `observe_only` / `safe_mode` default
- enger Diagnostics-Scope
- keine oeffentliche Auslieferung

## Gate

- First Installable Staging Pilot Package: `approval_required`
