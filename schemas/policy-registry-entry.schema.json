{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://electri-city-ops.local/schemas/policy-registry-entry.schema.json",
  "title": "Policy Registry Entry",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "registry_id",
    "license_id",
    "bound_domain",
    "release_channel",
    "policy_version",
    "default_mode",
    "allowed_scopes",
    "module_flags",
    "registry_state",
    "created_at"
  ],
  "properties": {
    "registry_id": {
      "type": "string",
      "minLength": 1
    },
    "license_id": {
      "type": "string",
      "minLength": 1
    },
    "bound_domain": {
      "type": "string",
      "minLength": 1
    },
    "release_channel": {
      "type": "string",
      "enum": ["stable", "pilot", "rollback"]
    },
    "policy_version": {
      "type": "string",
      "minLength": 1
    },
    "default_mode": {
      "type": "string",
      "enum": ["safe_mode", "observe_only", "approval_required", "active_scoped"]
    },
    "allowed_scopes": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "module_flags": {
      "type": "object"
    },
    "registry_state": {
      "type": "string",
      "enum": ["pending", "confirmed", "blocked", "approval_required"]
    },
    "created_at": {
      "type": "string",
      "minLength": 1
    }
  }
}
